# Copy to .envrc or source manually before running npm run start:http.
CODEXPRO_ROOT=/absolute/path/to/your/repo
# Optional named project catalog. When set, catalog roots replace CODEXPRO_ROOT as
# the connector's project set. See projects.example.json.
# CODEXPRO_PROJECTS_FILE=/absolute/path/to/projects.json

# Optional per-MCP-task Git worktree isolation. Each task calls create_workspace
# once and passes the returned workspace_id to every later repository tool call.
# CODEXPRO_WORKTREE_MODE=mcp
# CODEXPRO_WORKTREE_BASE=HEAD
# CODEXPRO_WORKTREE_ROOT=~/.codexpro/worktrees
# CODEXPRO_MAX_WORKTREES=64
CODEXPRO_ALLOWED_ROOTS=/absolute/path/to/your/repo
CODEXPRO_HOST=127.0.0.1
CODEXPRO_PORT=8787
CODEXPRO_BASH_MODE=safe
# Optional hard cap for bash tool timeout_ms. Default 600000 (10m), max 900000 (15m).
# CODEXPRO_MAX_BASH_TIMEOUT_MS=600000
# Optional ast-grep executable override. Normal npm installs use packaged @ast-grep/cli.
# CODEXPRO_AST_GREP_PATH=/absolute/path/to/ast-grep
# Max bytes for import_file ChatGPT attachment downloads. Default 5000000.
# CODEXPRO_MAX_IMPORT_BYTES=5000000
# Extra approved HTTPS hosts for import_file, comma-separated. Loopback stays blocked unless explicitly enabled for tests.
# CODEXPRO_IMPORT_ALLOWED_HOSTS=files.oaiusercontent.com
# CODEXPRO_IMPORT_ALLOW_LOOPBACK=0
# Optional local bash session guard. If required, bash tool calls must include the matching session_id.
# CODEXPRO_BASH_SESSION_ID=main
# CODEXPRO_REQUIRE_BASH_SESSION=1
CODEXPRO_WRITE_MODE=workspace
# AI-Bridge planning/handoff tools are hidden for the normal direct read/write/bash workflow.
# Set to on only when deliberately using execute-handoff/watch-handoff/loop-handoff.
CODEXPRO_HANDOFF_MODE=off
CODEXPRO_TOOL_MODE=standard
# Local absolute paths are replaced with labels ([workspace:...], [project:...],
# [home]) in MCP tool results by default. Enable only for trusted local debugging.
# CODEXPRO_EXPOSE_ABSOLUTE_PATHS=1
# MCP replies are always text-only; legacy tool-card settings are ignored.
# Optional metadata-only debug journal for direct MCP actions. It records tool/project/workspace,
# safe target metadata, opaque actor/request/session references, outcome, duration, and mutation evidence—but
# never file bodies, prompts, search/ast-grep pattern text or captures, command text, tokens, or raw output. activity_* tools are hidden unless enabled.
# CODEXPRO_AUDIT_MODE=metadata
# CODEXPRO_AUDIT_LOG=~/.codexpro/audit/tool-calls.jsonl
# CODEXPRO_AUDIT_MAX_BYTES=67108864
# CODEXPRO_AUDIT_RETAIN_ACTIONS=50000
# At least 24 UTF-8 bytes. Prefer the generated random token from codexpro setup/start.
CODEXPRO_HTTP_TOKEN=replace-with-a-long-random-token
# Require a token even for loopback binds. Non-loopback and tunnel mode require one unless explicitly overridden.
# CODEXPRO_REQUIRE_HTTP_TOKEN=1
# CODEXPRO_ALLOW_NO_HTTP_TOKEN=1
CODEXPRO_CONTEXT_DIR=.ai-bridge
# Optional stable public URL mode. Quick tunnels do not keep the same hostname.
# CODEXPRO_TUNNEL=cloudflare-named
# CODEXPRO_PUBLIC_HOSTNAME=codexpro.example.com
# CLOUDFLARE_TUNNEL_NAME=codexpro
# Or use a dashboard-managed tunnel token:
# CLOUDFLARE_TUNNEL_TOKEN_FILE=/Users/you/.codexpro/cloudflare-tunnel-token
# Optional comma-separated extra block list:
# CODEXPRO_BLOCKED_GLOBS=**/secrets/**,**/*.sqlite,**/*.db

# Managed jobs: absolute runtime, capture and retained-output budgets.
# CODEXPRO_JOB_TIMEOUT_MS=1500000
# CODEXPRO_MAX_JOB_OUTPUT_BYTES=8388608
# CODEXPRO_MAX_JOBS=12
# CODEXPRO_MAX_JOBS_PER_WORKSPACE=6
# CODEXPRO_JOB_RETENTION_MS=86400000
# CODEXPRO_MAX_RETAINED_JOB_BYTES=536870912
# CODEXPRO_MAX_JOB_HISTORY_PER_WORKSPACE=50
# See docs/JOBS.md for pages, managed file references and input_job_ids leases.

# Optional durable work coordinator (manual and Ralph runs). Keep stores separate.
CODEXPRO_WORK_MODE=off
# CODEXPRO_WORK_DIR=/absolute/private/path/codexpro-work
# CODEXPRO_WORK_MANAGEMENT=1
# CODEXPRO_WORK_IDLE_MS=600000
# CODEXPRO_WORK_ATTEMPT_MS=1500000
# CODEXPRO_WORK_MAX_ATTEMPTS=20
# CODEXPRO_WORK_MAX_ACTIVE_MS=7200000
# CODEXPRO_WORK_MAX_RUNS=100
# CODEXPRO_WORK_MAX_DOCUMENTS=200
# CODEXPRO_WORK_MAX_DOCUMENT_BYTES=131072
# CODEXPRO_WORK_MAX_RUN_DOCUMENT_BYTES=16777216
# CODEXPRO_WORK_SOURCE_MAX_BYTES=134217728
# CODEXPRO_WORK_PACKET_BYTES=16384
# CODEXPRO_WORK_SWEEP_MS=15000
